# Dev overlay — expose ports for local access
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d
services:
  redis:
    ports:
      - "6379:6379"

  claude-hub:
    ports:
      - "7700:7700"
